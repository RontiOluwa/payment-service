# Provides Redis, required for BullMQ-based asynchronous payment
# processing. This is NOT needed to run the API itself if you switch
# STORAGE_DRIVER to "memory" and don't need real queue processing —
# but for the default "file" + queue-based configuration, Redis must
# be running before `npm run start:dev`.
#
# Usage:
#   docker compose up -d      # start Redis in the background
#   docker compose down       # stop it
#   docker compose down -v    # stop it and delete persisted queue data
version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: payment-service-redis
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  redis-data:
